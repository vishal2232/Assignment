1. We can execute the shell script and provide url when ask
2. We can execute below command 
   sh capture.sh

3. If we need to scaleup we can use docker-compse for that
4. docker-compose up -d --scale webScreenshot=2 

